function printRange(rangeStart, rangeStop, skip) {
    for (var i = rangeStart; i < rangeStop; i=i+skip) {
       var text="";
       text+=i;
       console.log(text);
    }
}
printRange(2, 10, 2);

function printRange(rangeStart, rangeStop, skip) {
    for (var i = rangeStart; i < rangeStop; i=i+skip) {
      if (skip <2){

      }
      else{
       var text="";
       text+=i;
       console.log(text);
        
      }
    }
}
printRange(2, 10, 2);